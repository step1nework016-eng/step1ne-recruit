# 待盤點：兩位人選面談完之後

2026-07-30 ｜ 面談中累積的討論事項，等 呂皓宇 談完一起處理

---

## 一、面談時長要重新定義（有實測數字了）

張博州那場的實際數字：

| | 平均 | 範圍 |
|---|---|---|
| 阿財回覆 | **38 秒** | 0–111 |
| **候選人打字** | **178 秒** | 64–444 |
| 一來一回 | **216 秒 ≈ 3.6 分鐘** | |

**慢的是候選人，不是阿財**（阿財只佔 18%）。這推翻了原本「換 API 就會快」的假設。

| 方案 | 12 個來回 |
|---|---|
| 現在 | 43 分鐘 |
| 改走 API（阿財 6 秒） | 37 分鐘（只省 6 分鐘） |
| **改 API ＋ 壓到 8 個來回** | **25 分鐘** |

### 待決定

1. **來回次數要不要壓到 8 次？** 做法是一則訊息問兩件相關的事，
   而不是拆兩則各等三分鐘。改 SKILL 就好，不用動架構。
2. **開場與信件的時間怎麼寫？** 目前寫「20 到 30 分鐘」，
   但事實是 40 分鐘以上。壓到 8 個來回才寫得出 30 分鐘。
   ⚠️ **低估比高估傷** ——講 20 卻跑 40，人會中途走。

---

## 二、併發能力

這台是 **8GB／4 核**，`MAX_PARALLEL` 已從 5 降到 3。
今天同時兩場（張博州＋呂皓宇）時負載 **5.08**（超過核心數）、可用記憶體 2.4GB。

- 3 場是這台機器的上限，第 4 個人會排隊（不會被拒絕，但每輪多等一倍）
- 要撐 10 場同時，只有走 API（不佔本機資源）
- **「主動分流時段」是新功能**，需要時段管理機制。使用者傾向先不做——
  一天不會有 4 人同時，排隊撐得住

---

## 三、還沒上線的東西

| 項目 | 狀態 |
|---|---|
| 後台刪除紀錄按鈕 | Worker 端點做好了，**前端按鈕還沒上**（面談中不動聊天室相關頁面） |
| 測試資料清理 | `agent Ai`、`UI測試-黃志遠` 還在資料庫裡 |

---

## 四、使用者明確表達的原則

- **不會自動刪任何東西。** 對話與報告要永久保留。
  「個資保留期限自動清理」那件事繼續擱著（需要使用者決定期限）
- **面談進行中不要碰聊天室相關的程式碼**
- 面談連結與報告全文**放後台**，Telegram 只推「該不該去看」

---

## 五、今天已完成（不用再處理）

- 履歷改切塊存 D1，上限 8MB（實測 1.94MB PDF → 3 塊 → 抽出 5375 字乾淨中文）
- 移除瀏覽器 pdf.js（中文 PDF 會抽出大量空位元組）
- 三處加控制字元過濾（空位元組會讓整場面談炸掉）
- `--force` 保護（之前會清空「只有文字沒有檔案」的舊資料）
- 顧問後台：新增候選人、面談連結補救、即時逐字稿（5 秒）、報告下載、
  問題回報顯示、體驗評分顯示
- 面談室：問題回報按鈕、結束後五級表情評分、系統暗號不再外洩
- 逾時收尾會先留話再結束
- SKILL：開場講時長、超時主動收尾、收尾分兩則、他還有問題怎麼處理
- `force_close.py`：顧問手動叫阿財正式收尾（走 Phase 6，不用「你沒回覆」那套）
